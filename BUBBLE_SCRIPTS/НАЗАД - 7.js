function hideWheelOnBack() {
    const wheelContainer = document.getElementById('wheel-container');
    if (wheelContainer) {
        wheelContainer.style.display = 'none'; // Скрыть контейнер с колесом
    }
}